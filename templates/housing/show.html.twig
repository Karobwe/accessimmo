{% extends 'base.html.twig' %}

{% block title %}Housing{% endblock %}

{% block body %}
    <h1>Housing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ housing.id }}</td>
            </tr>
            <tr>
                <th>ShortDescription</th>
                <td>{{ housing.shortDescription }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ housing.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ housing.price }}</td>
            </tr>
            <tr>
                <th>RoomCount</th>
                <td>{{ housing.roomCount }}</td>
            </tr>
            <tr>
                <th>BedroomCount</th>
                <td>{{ housing.bedroomCount }}</td>
            </tr>
            <tr>
                <th>FloorArea</th>
                <td>{{ housing.floorArea }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('housing_index') }}">back to list</a>

    <a href="{{ path('housing_edit', {'id': housing.id}) }}">edit</a>

    {{ include('housing/_delete_form.html.twig') }}
{% endblock %}
